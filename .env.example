# HTTP Basic Auth credentials
# BASIC_AUTH_USER defaults to "admin" if not set.
# BASIC_AUTH_PASSWORD is required — the container will not start without it.
BASIC_AUTH_USER=admin
BASIC_AUTH_PASSWORD=changeme

# Port to expose the web app on the host
APP_PORT=8080
